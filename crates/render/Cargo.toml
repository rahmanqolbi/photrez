[package]
name = "photrez-render"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "22.0.0"
bytemuck = { version = "1", features = ["derive"] }
log = "0.4"

[dev-dependencies]
pollster = "0.3.0"
